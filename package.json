{
  "name": "aifactory-demo",
  "version": "0.1.0",
  "description": "FastAPI demo service — inventory reservation and link shortener.",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "uvicorn src.app.main:app --reload",
    "test": "playwright test games/tictactoe/evidence.spec.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "canvas": "^3.2.3",
    "http-server": "^14.1.1",
    "sharp": "^0.35.3"
  }
}
