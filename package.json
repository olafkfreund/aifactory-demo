{
  "name": "aifactory-demo",
  "version": "0.1.0",
  "description": "FastAPI demo service — inventory reservation and link shortener.",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "uvicorn src.app.main:app --reload",
    "test": "pytest -q",
    "test:a11y": "playwright test games/tictactoe/a11y.spec.js"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.8.0",
    "@playwright/test": "^1.40.0",
    "axe-core": "^4.13.0",
    "axe-playwright": "^2.2.2",
    "jsdom": "^30.0.1"
  }
}
