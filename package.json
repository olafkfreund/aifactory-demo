{
  "name": "aifactory-demo",
  "version": "0.1.0",
  "description": "FastAPI demo service — inventory reservation and link shortener.",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "uvicorn src.app.main:app --reload",
    "test": "pytest -q",
    "test:evidence": "playwright test games/tictactoe/evidence.spec.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "canvas": "^2.11.2",
    "fluent-ffmpeg": "^2.1.3"
  }
}
